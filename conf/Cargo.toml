[package]
name = "conf"
version.workspace = true
edition.workspace = true

[dependencies]
serde = { version = "1.0", features = ["derive"] }
tracing = "0.1"
uuid.workspace = true

[dev-dependencies]
serde_json = "1.0"
